[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "churn-mlops"
version = "0.1.0"
description = "End-to-end MLOps project: churn prediction with training, orchestration, deployment and monitoring"
readme = "README.md"
requires-python = ">=3.10,<3.13"
license = { text = "MIT" }
authors = [
    { name = "Do Pham Ngoc", email = "ngocdo992k4@gmail.com" }
]

dependencies = [
    "pandas==2.2.2",
    "numpy==1.26.4",
    "scikit-learn==1.5.1",
    "xgboost==2.1.0",
    "mlflow==2.15.1",
    "prefect==2.20.2",
    "fastapi==0.112.0",
    "kafka-python==2.0.2",
    "uvicorn[standard]==0.30.5",
    "pydantic==2.8.2",
    "pydantic-settings==2.4.0",
    "evidently==0.4.33",
    "boto3==1.34.155",
    "python-dotenv==1.0.1",
    "kagglehub>=1.0.2",
    "dvc>=3.67.1",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.2",
    "pytest-cov==5.0.0",
    "httpx==0.27.0", 
    "ruff==0.5.7",
    "pre-commit==3.8.0",
]

[project.scripts]
run-training = "scripts.run_training:main"
run-batch-scoring = "scripts.run_batch_scoring:main"

# ---- setuptools: src-layout ----
[tool.setuptools.packages.find]
where = ["src"]
include = ["churn_mlops*"]

# ---- pytest ----
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q --cov=churn_mlops --cov-report=term-missing"

# ---- ruff ----
[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
